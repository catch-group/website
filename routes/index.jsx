import { define } from "../utils.js";

const Home = define.page(() => (
	<p>
		<blockquote>I don't know how distant it is, but in the future a website for the Catch Group will be here.</blockquote>

		<span>- Me, while writing this</span>
	</p>
));

export default Home;
